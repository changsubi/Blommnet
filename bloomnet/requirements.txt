# ═══════════════════════════════════════════════════════════════════════
#  런타임 (06 §5.0, 정정 A-13)
#  ★ 현 venv(.venv)로 **즉시 충족**된다.
#    T01~T24 중 onnx 무관한 전부가 이 목록만으로 실행된다.
#  ★ 헌법 C-5.3 — 인터넷 의존 금지. 사전학습 가중치 다운로드는 어떤 경로에도 없다.
#    timm/transformers 는 아키텍처 참조용이며 가중치 fetch 를 하지 않는다.
# ═══════════════════════════════════════════════════════════════════════
torch>=2.0
torchvision
numpy
pillow
pyyaml
tqdm
scipy            # utils/metrics_boundary.py 의 distance_transform_edt (BF-score, 01 §8.1)
timm             # 블록 참조 구현 대조용 (가중치 다운로드 안 함)
transformers     # 동일

# cv2 는 **어느 목록에도 넣지 않는다** — 01 §7.5 경계 연산자를 torch max_pool 로 대체해
# 의존이 사라졌다 (06 §5.0).
